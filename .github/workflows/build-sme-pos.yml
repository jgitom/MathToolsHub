name: Build SME POS desktop installer

on:
  workflow_dispatch:
  push:
    tags:
      - "sme-pos-v*"

permissions:
  contents: read

jobs:
  build:
    runs-on: windows-latest
    defaults:
      run:
        working-directory: apps/sme-pos-desktop
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-node@v4
        with:
          node-version: 22
          cache: npm
          cache-dependency-path: apps/sme-pos-desktop/package-lock.json
      - run: npm ci
      - run: npm run dist:win
        env:
          GH_TOKEN: ${{ secrets.GITHUB_TOKEN }}
          CSC_IDENTITY_AUTO_DISCOVERY: "false"
      - uses: actions/upload-artifact@v4
        with:
          name: sme-pos-windows
          path: apps/sme-pos-desktop/dist/*
          if-no-files-found: error
