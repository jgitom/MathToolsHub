name: Build CJ PDF to Word desktop installer

on:
  workflow_dispatch:
  push:
    tags:
      - "cj-pdf-v*"

permissions:
  contents: read

jobs:
  build:
    runs-on: windows-latest
    defaults:
      run:
        working-directory: apps/cj-pdf-desktop
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-node@v4
        with:
          node-version: 22
          cache: npm
          cache-dependency-path: apps/cj-pdf-desktop/package-lock.json
      - run: npm ci
      - run: npm run dist:win
        env:
          GH_TOKEN: ${{ secrets.GITHUB_TOKEN }}
          CSC_IDENTITY_AUTO_DISCOVERY: "false"
      - uses: actions/upload-artifact@v4
        with:
          name: cj-pdf-windows
          path: apps/cj-pdf-desktop/release/*
          if-no-files-found: error
