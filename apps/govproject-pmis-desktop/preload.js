// GovProject PMIS — preload (isolated context)
// The app is fully client-side (IndexedDB + localStorage), so no Node bridge is required.
// Kept for future-proofing (e.g. native file dialogs, printing helpers).
