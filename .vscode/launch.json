{
    "version": "0.2.0",
    "configurations": [
        {
            "name": "Debug Current HTML File",
            "type": "pwa-chrome",
            "request": "launch",
            "file": "${file}",
            "webRoot": "${workspaceFolder}"
        }
    ]
}