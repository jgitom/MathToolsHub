import { createClient } from "https://cdn.jsdelivr.net/npm/@supabase/supabase-js@2/+esm";
const config=window.MATHTOOLSHUB_SUPABASE,sections={primary:document.querySelector("#primary-title")?.closest(".catalogue-section"),secondary:document.querySelector("#secondary-title")?.closest(".catalogue-section")};
if(config?.url&&config?.publishableKey&&sections.primary&&sections.secondary){const client=createClient(config.url,config.publishableKey,{auth:{persistSession:true,autoRefreshToken:true,detectSessionInUrl:true}});const {data,error}=await client.from("catalogue_items").select("activity_id,title,description,url,section,icon,sort_order").eq("active",true).order("sort_order");if(!error&&data?.length){for(const sectionName of ["primary","secondary"]){const section=sections[sectionName],items=data.filter(item=>item.section===sectionName),list=section.querySelector(".page-list");list.innerHTML=items.map(item=>`<a class="page-card" href="${item.url}" data-activity-id="${item.activity_id}"><span class="card-number" aria-hidden="true">${item.icon||"∑"}</span><span class="card-copy"><strong>${item.title}</strong><span>${item.description}</span></span><span class="arrow">›</span></a>`).join("");section.querySelector(".count").textContent=`${items.length} PROGRAMMES`;}const selector=document.getElementById("languageSelect");if(selector)selector.dispatchEvent(new Event("change",{bubbles:true}));}}
